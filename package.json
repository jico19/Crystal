{
  "name": "crystal-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Crystal Multi-State Home Care Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:setup": "npm --prefix apps/api run db:setup",
    "build": "npm run build --workspaces --if-present"
  }
}
